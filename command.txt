git config --global user.email"rajveer.rajveerparia@gmail.com"
git config --global user.name"rajveer"

git init
git add [filename] || -A
git status
git commit -m "msg"
git remote add origin repoName
git push origin master
git clone repoName
giot pull origin master